# canvas — INDEX

Per-BC catalog. See `README.md` for purpose, classification, ubiquitous language.

## ADRs

<!-- adr-list:start -->
- [ADR-021 — Retire `bc_positions`; add `bc_view_state` for accordion collapse + reorder](../../knowledge/decisions/ADR-021-retire-bc-positions-for-bc-view-state.md) — Accepted. `scope: bc`. **canvas-023's persistence decision.** The `bc_positions` table (schema v4, project-registry-004) is dead surface since canvas-020 retired BC bubbles; rather than `ALTER TABLE` it, schema **v5→v6 DROPs `bc_positions` and creates `bc_view_state (project_id, bc_name, collapsed, sort_order)`** (retire-and-recreate over column-add — the old shape carried `x/y` that no longer mean anything). Per-`(project_id, bc_name)` accordion collapse + drag-reorder `sort_order` persist in GUPPI's own SQLite (ADR-004); unset `sort_order` defaults stably to BC-name order. Plain `save_/load_bc_view_state` + batch IPC round-trips (no DomainEvent — no second consumer, so ADR-009's bus is intentionally not used). Data-preservation migration test proves other tables survive. Authored 2026-05-24 by canvas-023. [canvas-023]
- [ADR-020 — Task-card selection signal + the live-agent elapsed clock](../../knowledge/decisions/ADR-020-task-card-selection-and-elapsed-clock.md) — Accepted. `scope: bc`. **canvas-021's card-content decisions.** Card click sets a local `selectedTask` rune AND emits an `onTaskSelected` prop callback — the outbound "open detail panel for this task" seam canvas-022's panel host plugs into (component owns selection state so the §3.11 `selected` border renders without a panel consumer wired yet). The per-card live-agent elapsed string ("waiting 2m 14s") advances from a **single canvas-wide 1 Hz local clock gated on live presence** (`hasLiveAgent`), timing against agent-awareness-002's `since` timestamp supplied once — no per-second event, no per-card timer, no tick while idle. Authored 2026-05-24 by canvas-021. [canvas-021]
- [ADR-019 — Kanban-accordion DOM-interior overlay — camera-version reactive bridge + fixed frame shell](../../knowledge/decisions/ADR-019-kanban-accordion-interior-overlay.md) — Accepted. `scope: bc`. **Implements ADR-017's hybrid substrate.** Each project frame's BC accordion → kanban board → task cards renders as a DOM overlay positioned via `camera.worldToScreen` + `transform: scale(z)`; the Pixi shell (border/header/drag/missing-tile) survives. Accordion collapse defaults all-expanded in memory (persistence canvas-023). Viewport-cull `CULL_MARGIN_PX=120` + zoom-LOD `LOD_ZOOM_FLOOR=0.45`. Targeted per-task updates ride the existing `applyDomainEvent` patch path (canvas-001 at task granularity). Authored 2026-05-24 by canvas-020. [canvas-020]
- [ADR-017 — Rendering substrate for the kanban-accordion frame interior — hybrid Pixi shell + DOM interior](../../knowledge/decisions/ADR-017-rendering-substrate-kanban-accordion-interior.md) — Accepted. **Kanban-accordion pivot substrate decision (ADR-003 revisit).** Ratifies **hybrid**: Pixi keeps the camera/world transform (ADR-016) + the frame shell (border/header/drag hit-area); each frame's kanban-accordion interior + the docked detail panel render as HTML/DOM overlays positioned via `camera.worldToScreen`. Cites `canvas-019a`'s release-build PASS (N=10 frames, all interiors mounted, `p95 = 8.5 ms`, card-density-independent — `research/canvas-hybrid-perf-2026-05-24`). Records the viewport-culling (`CULL_MARGIN_PX = 120`) + zoom-threshold LOD (`LOD_ZOOM_FLOOR = 0.45`) policy. Survivors: `camera.svelte.ts`, `drag-controller.ts`, `tile-layout.ts`; retired: `bc-layout.ts` + the Pixi BC-bubble + intra-project-edge draw paths in `Canvas.svelte`. Extends ADR-003; supersedes-in-part ADR-015 (canvas-layout consequence). Option B (full-DOM) costed fallback, Option C (pure-Pixi) rejected. Authored 2026-05-24 by canvas-019.
- [ADR-016 — Persistent PixiJS scene graph + camera as stage transform](../../knowledge/decisions/ADR-016-persistent-scene-graph.md) — Accepted. Each project frame's `Container` / `Graphics` / `Text` instantiated once and updated in place; pan = `world.position.set(...)`, wheel-zoom = `world.scale.set(...)` + `world.position.set(...)` + in-place stroke-width `repaint()`. Authored 2026-05-19 by canvas-015 (commit `9257660`). The camera + frame-shell half survives the kanban-accordion pivot (ADR-017); the Pixi BC-bubble + intra-project-edge draw paths it describes retire.
- [ADR-015 — BC layout inside a project frame — deterministic one-shot spring-electrical with sticky pins](../../knowledge/decisions/ADR-015-bc-layout-deterministic-spring-electrical.md) — **Superseded-in-part by ADR-017.** Pure deterministic spring-electrical placement (mulberry32 seeded by `project_id`), one-shot on input change, manual drags pinned in subsequent re-layouts. The **canvas-layout consequence** (`bc-layout.ts` force-directed bubble placement) is superseded by the kanban-accordion pivot; the **README-frontmatter relationship data model** (ADR-014, owned by `project-registry-004`) is NOT superseded and stays in force.
<!-- adr-list:end -->

## Backlog

<!-- backlog-list:start -->
- [canvas-022-docked-detail-panel](backlog/canvas-022-docked-detail-panel.md) — `type: feature`, depends on `canvas-019` + `canvas-021` + `design-system-006` + `project-registry-005` + `agent-awareness-002`. Right-edge viewport-docked task detail panel that animates in/out on card click; hosts the "AGENT NEEDS AN ANSWER" blocked callout (re-homes the deleted `agent-awareness-001` concern). Created 2026-05-24.
- [canvas-016-cache-screen-space-overlays](backlog/canvas-016-cache-screen-space-overlays.md) — `type: feature`, depends on `canvas-014`. Cache the voice indicator (`makeVoiceIndicator`) as a per-mount `Container` instead of allocating fresh `Container` + `Graphics` + `Text` on every `renderScene`; same pattern for forthcoming `agent-awareness` badges. Estimate ½ day. Created by canvas-014 (2026-05-18).
- [canvas-017-broad-phase-hit-rejection-for-empty-canvas-pan](backlog/canvas-017-broad-phase-hit-rejection-for-empty-canvas-pan.md) — `type: feature`, depends on `canvas-014`. Set `world.eventMode = 'passive'` and a `world.hitArea` covering the union-bounds of every project frame so `pointermove` over empty canvas short-circuits Pixi's child-by-child hit walk. Estimate ½ day. Created by canvas-014 (2026-05-18).
- [canvas-018-dev-only-diagnostic-seam](backlog/canvas-018-dev-only-diagnostic-seam.md) — `type: feature`. Dev-only seam (`window.__guppi = { app, camera, ... }` behind an `import.meta.env.DEV` guard or similar) so future perf spikes / debugging sessions can read renderer state from the Tauri devtools console without source-patching. Surfaced by canvas-014's reproducer protocol friction. Created by canvas-014 (2026-05-18).
- [canvas-003-focus-zoom](backlog/canvas-003-focus-zoom.md) — `type: feature`, depends on `design-system-001` + `canvas-007`. Click-or-keyboard "zoom to focus" — camera frames the project's bounded region (option "B" locked 2026-05-15). **Structurally unblocked 2026-05-16** by canvas-007. Design pins from `references/claude-design-2026-05-16/` appended to task notes (overview 38% / focused 110% / 320ms / `cubic-bezier(.16,.84,.36,1)` / focus-ring brand orange). v1 core.
<!-- backlog-list:end -->

## Todo

<!-- todo-list:start -->
*(None.)*
<!-- todo-list:end -->

## Doing

<!-- doing-list:start -->
*(None.)*
<!-- doing-list:end -->

## Done

<!-- done-list:start -->
- [canvas-023-accordion-collapse-and-bc-reorder-persistence](done/canvas-023-accordion-collapse-and-bc-reorder-persistence.md) — `feature`, commit `9a7b55f`. Per-`(project_id, bc_name)` accordion **collapse** state + drag-**reorder** `sort_order` now persist in GUPPI's own SQLite (ADR-004) and survive restart. Schema **v5→v6**: DROP the orphaned `bc_positions` table (dead since canvas-020 retired BC bubbles) and create `bc_view_state (project_id, bc_name, collapsed, sort_order)` — retire-and-recreate, with a data-preservation migration test (projects/tile_positions/preferences survive). `save_/load_bc_view_state` + batch `load_bc_view_states` IPC follow the project-registry-004 pattern; frame paint batch-loads (`primeBcViewState` in `refresh`/`refreshOne`). Unset `sort_order` defaults stably to BC-name order; concurrent-save race-freedom tested. Authored ADR-021. `pnpm check` 0/0/0, `cargo test --lib` 149 green (11 new). Verifier: PASS iter 1. **Cross-BC note:** the `bc_positions` retire touches project-registry's DB surface — flagged for project-registry's owner (project-registry-004 introduced it). (2026-05-24)
- [canvas-021-task-card-rendering](done/canvas-021-task-card-rendering.md) — `feature`, commit `721f5a3`. Task-card CONTENT inside the kanban-accordion interior: id + 2-line-clamped title + tag chips (canvas-020 structure) + the **live-agent indicator line** ("orchestrator · waiting 2m 14s") — agent label + a locally-timed elapsed string from agent-awareness-002's `since` (running = brand-blue `▶` with the one sanctioned `durationPulse` breathe; blocked = static red `◆`). Four §3.11 card states (default/hover/selected/blocked). Card click sets `selectedTask` + emits `onTaskSelected` — the "open detail panel for this task" signal canvas-022 consumes. `task_agent_state_changed` patches the per-task read model in place (no resync, no IPC re-fetch); a single canvas-wide 1 Hz clock gated on `hasLiveAgent` advances elapsed (no per-second event, no tick while idle). Authored ADR-020. `pnpm check` 0/0/0, `pnpm test` 40/40 (6 new `formatElapsed` tests). Verifier: PASS iter 1. Unblocks canvas-022. (2026-05-24)
- [canvas-024-retire-spike-019a-harness](done/canvas-024-retire-spike-019a-harness.md) — `chore`, commit `cef866b`. Deleted the throwaway `src/routes/spike-019a/` perf harness (686 lines) + the `window.__guppiSpike` instrumentation seam now that canvas-020 landed the real interior and canvas-019 ratified hybrid (ADR-017). No dangling refs in `src/`; `pnpm check` clean + `pnpm build` succeeds. The findings note `research/canvas-hybrid-perf-2026-05-24/` + `console-snapshot.md` stay as knowledge artifacts. Verifier: PASS iter 1. (2026-05-24)
- [canvas-020-accordion-kanban-frame-interior](done/canvas-020-accordion-kanban-frame-interior.md) — `feature`, commit `7046e1f`. **Pivot spine landed.** Each project frame keeps its PixiJS shell while the interior renders as a DOM overlay (`camera.worldToScreen` + `transform: scale(z)`): a vertical accordion of collapsible BC rows, each a four-column BACKLOG/TODO/DOING/DONE kanban board of task cards (project-registry-005 records by `task.column`). Accordion all-expanded in-memory default (persistence canvas-023); header active/blocked/idling roll-up wired to agent-awareness-002's `get_bc_agent_rollup` + patched on `task_agent_state_changed`. ADR-017 culling (`CULL_MARGIN_PX=120`) / LOD (`LOD_ZOOM_FLOOR=0.45`). Retired the Pixi BC-bubble + intra-project-edge draw paths and DELETED `bc-layout.ts` (+ its test). New pure `frame-interior.ts` (11 tests). Authored ADR-019. `pnpm check` 0/0/0, `pnpm test` 34/34, `cargo test --lib` unchanged. Verifier: PASS iter 1. Unblocks canvas-021/022/023. (2026-05-24)
- [canvas-019-rendering-substrate-decision-kanban-interior](done/canvas-019-rendering-substrate-decision-kanban-interior.md) — `decision` — **Kanban-accordion pivot, gate task — ADR-003 revisit resolved.** Ratified **hybrid** (Pixi camera/world transform + frame shell; kanban-accordion interior + docked detail panel as DOM overlays positioned via `camera.worldToScreen`) as **ADR-017** (`scope: bc, canvas`), citing `canvas-019a`'s release-build PASS (N=10 frames, all interiors mounted, `p95 = 8.5 ms`, card-density-independent). Recorded the viewport-culling (`CULL_MARGIN_PX = 120`) + zoom-threshold LOD (`LOD_ZOOM_FLOOR = 0.45`) policy and the perf stance for N≈10 full frames; enumerated survivors (`camera.svelte.ts`, `drag-controller.ts`, `tile-layout.ts`) vs retired (`bc-layout.ts` + the Pixi BC-bubble + intra-project-edge draw paths in `Canvas.svelte`). Marked ADR-015 Superseded-in-part (canvas-layout consequence only; the ADR-014 README-frontmatter relationship data model survives) and added the two affected `README.md` vocabulary forward-pointers. Option B (full-DOM) costed fallback, Option C (pure-Pixi) rejected. Unblocks canvas-020/021/022. Verifier: PASS iter 1. (2026-05-24)
- [canvas-019a-perf-spike-hybrid-dom-interior](done/canvas-019a-perf-spike-hybrid-dom-interior.md) — `spike` — Throwaway, instrumented Option A (hybrid Pixi-shell + DOM kanban-interior) perf harness at `src/routes/spike-019a/+page.svelte`: N≈10 project frames, each a Pixi shell (border + header, world coords, stroke pre-divided by `z` per ADR-016 §3) carrying a DOM kanban-accordion interior (5 BCs × 3 BACKLOG→DONE columns × 8 dummy cards) positioned via the `world·zoom + pan` screen contract. **Viewport culling**: screen-space AABB intersection + `CULL_MARGIN_PX` — off-screen frames render Pixi-shell-only. **Zoom-threshold LOD**: `LOD_ZOOM_FLOOR = 0.45` suppresses the DOM interior below the floor (Pixi summary shell) and re-mounts on zoom-in. Instrumented with a rolling rAF frame-time sampler + `window.__guppiSpike` dev seam (`autopan(5)` 5s pan-circle, `stats()`, `reset()`). Findings note `research/canvas-hybrid-perf-2026-05-24/` records the chosen thresholds + rationale, an explicit verdict rule (`p95 ≤ 16 ms` PASS), the FAIL→Option-B (full-DOM) escape, and residual-risk surprises (mount-spike on culling re-entry); `authored_for: canvas-019`. **Frame-time numbers are OPERATOR-PENDING** — the Tauri dev shell / real GPU cannot be driven from a worker shell (canvas-014 precedent); the note carries a Marco-runnable reproducer protocol and an empty `console-snapshot.md` stub, no fabricated numbers. Does not touch production `Canvas.svelte`. New backlog `canvas-024` to retire the harness once `canvas-019` ratifies. `pnpm check` 0/0 (992 files). Verifier: PASS iter 1. (2026-05-24)
- [canvas-015-persistent-scene-graph-and-stage-transform-pan](done/canvas-015-persistent-scene-graph-and-stage-transform-pan.md) — `feature` — Renderer rewritten as a persistent PixiJS scene graph with `world.position` / `world.scale` carrying the camera transform. Per project frame, each `Container` / `Graphics` / `Text` is instantiated once (on `project_added` or initial `refresh`) and updated in place on subsequent renders — no more `world.removeChildren()` + ~200–500 fresh allocations per render. Pan triggers only `world.position.set(...)` + GPU draw (zero Pixi allocation in the per-pan path); wheel-zoom is `world.scale.set(...)` + `world.position.set(...)` + an in-place `repaint()` pass that `.clear()`s and redraws stroke widths so border-category strokes stay screen-space at any zoom (ADR-003 Extension invariant #4). Theme flip rides the same `repaint()`. Missing-tile state, hover focus ring, and intra-project edge geometry all update via property writes (`.visible`, `.tint`, `.position`, `.clear()` + redraw), never reinstantiation. ADR-015's BC-layout / sticky-pin contract preserved — drag-end calls `recomputeBcLayout(entry)` one-shot and the persistent edge `Graphics` reflows. ADR-003 Extension 2026-05-19 invariant #6 (BC text floor — "every BC always shows its name if you squint") preserved via a per-`Text` counter-scale at the four title sites (project title, BC title, BC counts pill text, missing-tile glyph): `screenSpaceTitleScale(fontSize, z) = Math.max(1, 8 / (fontSize * z))` applied via `.scale.set(...)` BEFORE `truncateTextToWidth`, so on-screen size is clamped to 8 CSS px at extreme zoom-out (verified math: `sizeBody=14` at `z=0.2` → `text.scale = 2.857` → rendered = `14×0.2×2.857 = 8` CSS px). The counter-scale runs only on `world.scale` changes (in `repaint()`), never per pan-tick, so the zero-allocation pan invariant survives the floor's restoration. ADR-016 §4 "Text scaling" (new) + canvas/README.md "Title-fits-frame invariant" entry both document the mechanism. Authored ADR-016 (`scope: bc, canvas`) covering the persistent scene-graph shift + the stroke-width-pre-divide-by-`z` policy + the hit-area model. `pnpm check` 0/0 (990 files); `pnpm test` 29/29 (infrastructure-017's vitest suite); `cargo test --lib` 122/122. Verifier: PASS iter 2 (iter 1 FAIL on the floor regression, fixed in iter 2 via route (a)). **v1 canvas core now structurally complete — pan/zoom are pure camera-transform ops, the substantive perf rewrite is in place; canvas-016 / 017 / 018 follow-ups still apply.** (2026-05-19)
- [canvas-012-drag-state-stickiness](done/canvas-012-drag-state-stickiness.md) — `bug` — Canvas drag controller extracted into a pure `src/lib/drag-controller.ts` module (verification-surface peer of `tile-layout.ts` / `bc-layout.ts` / `snapshot-patch.ts`, zero Svelte + zero Pixi imports). Replaces the four-variable drag-state spread (`dragProjectId` / `dragBcName` / `dragOriginX/Y` at module scope + `panning` at function scope) with a single discriminated-union `DragState` (`idle` | `panning` | `frame` | `bc`) and a `Target` descriptor (`empty` | `frameHeader` | `bcBubble`); five transition fns (`onPointerDown` / `onPointerMove` / `onPointerUp` / `onPointerCancel` / `onPointerLeave`). The canonical stuck-pan repro (clicking a frame body, releasing, then moving the mouse) is closed by the state machine alone — `onPointerUp` lands in IDLE for every state kind, unconditionally. New `window` `pointercancel` + `pointerleave` listeners wired as terminal events. `button === 2` short-circuit keeps right-clicks from entering a drag. Frame `hitArea` (header-bar-only) unchanged — the frame-body pass-through that lets empty regions inside a frame pan the camera (per canvas-007) is preserved. Pointer Capture API migration deliberately OUT (audit confirmed: a one-line unconditional-clear + two missing terminal listeners is not a structural failure of the window-listener model). `pnpm check` 0/0 (990 files); `cargo test --lib` 122/122. Verifier: PASS iter 1. (2026-05-19)
- [canvas-013-crisp-rendering-constant-size-project-titles](done/canvas-013-crisp-rendering-constant-size-project-titles.md) — `feature` — Three invariants for canvas rendering, captured as an **extension to ADR-003** (scope `global`, unchanged): (1) **Crispness invariant** — `app.init({ resolution: window.devicePixelRatio, autoDensity: true, … })` at the single boot site (Canvas.svelte L533); every `Text` and `Graphics` rasterises at the framebuffer's real resolution rather than the previous default-1× upscale. (2) **Constant-screen-size project titles (Miro-style)** — project frame titles render as **HTML overlays** in the existing ADR-003 overlay container, at a constant CSS pixel size (`var(--guppi-size-title)`) at every camera zoom; end-truncates via CSS `text-overflow: ellipsis` against the current frame header width (`bcLayout.width * camera.zoom`), no per-frame JS measurement; z-index 5 (above canvas, below interactive overlays at 10/11/20); `pointer-events: none` so it never swallows pan/drag/right-click/wheel-zoom; subscribes to camera + projects + theme via Svelte 5 runes. The Pixi `Text` allocation that previously held the project title is removed from `drawProjectFrame`, shedding one per-render rasterisation. (3) **Screen-space stroke widths for the borders category** — frame border, BC bubble border, header divider, focus ring (project frame + BC), and all four intra-project edge variants (incl. arrowheads `headLen`/`headW` + ACL notch size) drop the `* z` multiplier; widths are constant CSS pixels (autoDensity handles device-px). Caveats baked in: focus-ring **inset** stays world-space; arrowhead `pullBack` stays world-space because the bubble is drawn at `bcInsideWidth * z` in screen space. BC bubble title + task-count pill + frame header counts label stay in Pixi `Text` and scale with the camera, gaining crispness from the DPR fix (BC text floor + BitmapText-deferred stances both formally documented in the ADR). canvas/README.md gains two new vocabulary entries (Project-title overlay, Crispness invariant) and annotates Missing tile with the title-overlay opacity link. ADR-003 extension dated `## Extension 2026-05-19`. `pnpm check` 0/0/0 (940); `cargo test --lib` 122/122. Verifier: PASS iter 1. Commit `3315a10`. (2026-05-19)
- [canvas-014-investigate-pan-zoom-performance](done/canvas-014-investigate-pan-zoom-performance.md) — `spike` — Pan/zoom sluggishness investigated: the PixiJS ticker was unconditionally rebuilding the entire scene graph (`world.removeChildren()` + ~200–500 fresh `Graphics`/`Text`/`Container` allocations for N≈7 frames × 5 BCs) ~60 Hz, idle and during gestures alike. Trivial win landed: ticker callback guarded by `cameraTarget` (steady-state per-frame cost drops to zero), explicit `window` resize listener, explicit `renderScene()` added to the targeted-event default branch as the load-bearing correctness fix for count ticks / bc-appeared / bc-disappeared. Research report at `.agentheim/knowledge/research/canvas-perf-2026-05-17/` ranks three remaining hotspots (full rebuild per `renderScene`, screen-space overlay allocations, no broad-phase hit rejection) with effort estimates + per-hotspot proposed fixes + Marco-runnable reproducer protocol (Chrome trace + JS console snapshot). Four follow-up backlog tasks (canvas-015 / 016 / 017 / 018). canvas-013's strategy unchanged by findings — HTML-overlay-for-titles holds, BitmapText still deferred. ADR-003 + ADR-015 honoured; canvas-013's AC #1 (DPR fix at L533) deliberately untouched. `pnpm check` 0/0/0 (940); `cargo test --lib` unchanged (122/122). Verifier: PASS iter 1. Commit `eea3d09`. (2026-05-18)
- [canvas-008-apply-revised-tokens](done/canvas-008-apply-revised-tokens.md) — `feature` — Token-application audit pass against the 2026-05-16 design. `Canvas.svelte` + `bc-layout.ts` + `tile-layout.ts` + `snapshot-patch.ts` all consume tokens cleanly: zero inline hex literals, zero inline dimensional literals; the new `bcInsideWidth/Height` 188×60 and `frameHeaderHeight` 36 flow through correctly. Theme flip verified end-to-end — `initTheme()` runs pre-Pixi-init so first paint already lands in the persisted palette; `onThemeChange()` calls `renderScene()` + resets `renderer.background.color` so every PixiJS object picks up the active palette. **One audit catch:** `Modal.svelte`'s inline `rgb(22 22 28 / 70%)` backdrop literal was a stale dark-theme assumption — fixed with a new theme-invariant `--guppi-modal-backdrop` / `modalBackdrop` token (RGBA scrim per the design's §5 footnote). New backlog item `design-system-005` to document this token in `STYLEGUIDE.md`. `pnpm check` 0/0/0 (940); `cargo test --lib` 122/122. Verifier: PASS iter 1. **Closes the v1 design-refresh trio (003 → 004 → 008).** (2026-05-16)
- [canvas-004-styleguide-visuals](done/canvas-004-styleguide-visuals.md) — `feature` — **Subsumed by `canvas-008-apply-revised-tokens` (no work, no commit).** Originally captured as "replace greybox tiles/nodes/edges/counts/badge-slot with `STYLEGUIDE.md` tokens". `canvas-002` retired the greybox path; `canvas-007` (commit `e2296c2`) shipped the project-frame rendering against §3.6–3.8 tokens. The residual scope — re-applying the **revised** tokens after the 2026-05-16 design's brand-color + status + dimensional update — moved to `canvas-008`. Closure note in task body preserves audit trail. (2026-05-16)
- [canvas-007-project-as-frame](done/canvas-007-project-as-frame.md) — `feature` — Project tiles now render as bounded frames whose header bars carry project name + task counts and whose interior holds BCs as denser bubbles connected by intra-project relationship edges (four §3.8 variants on the locked `fgMuted` palette; geometry distinguishes customer-supplier / shared-kernel / ACL / conformist). New pure deterministic spring-electrical BC layout module (`bc-layout.ts`) places bubbles inside each frame with sticky per-BC drag positions via `project-registry-004`'s `saveBcPosition` IPC; recomputed one-shot on every topology change. `bc_relationships_changed` patches in place via `refreshOne(project_id)` — `canvas-001` targeted-update invariant preserved. `BcSnapshot` TS alias retired in favour of `BoundedContext`. ADR-015 captures the layout choice. `pnpm check` 0/0/0 (939 files); `cargo test --lib` 117/117. Commit `e2296c2`. (2026-05-16)
- [canvas-005b-scan-flow-and-scan-root-management](done/canvas-005b-scan-flow-and-scan-root-management.md) — `feature` — Shipped the scan-folder flow (folder picker → `addScanRoot` → discovery checklist modal: greyed-pre-ticked-disabled already-imported rows + "imported" badge + Select-all/none over togglable rows + disabled-on-zero-new-picks Import button → `importScannedProjects`, tiles arrive via canvas-006's `liveAddChain` at distinct spiral slots) and the scan-roots management surface (hidden-when-empty menu entry, list with per-row child counts via new thin `list_projects_by_scan_root` IPC wrapper, Rescan opens checklist flagged " (rescan)", Remove opens cascade-confirmation that names the path + N child count + explicit "tile state will not be retained" warning). New generic `Modal.svelte` primitive extracts the header/body/footer/backdrop/Escape contract for all three modals (extraction triggered by three consumers — styleguide entry deliberately deferred per task scope). `project_removed` handler from 005a reused without duplication; cascade fan-out drops tiles cleanly. `pnpm check` 0/0/0 (938 files). Commit `4979e27`. (2026-05-15)
- [canvas-005a-single-shot-discovery-affordances](done/canvas-005a-single-shot-discovery-affordances.md) — `feature` — Shipped ADR-005's three single-shot discovery affordances: right-click empty canvas → context menu with "Add project…" (Tauri folder picker → `registerProject` → rejection surfaces exact `"not an Agentheim project"` toast); right-click tile → "Remove project" (no confirmation, ADR-005's 30-day undo as safety net); missing-tile rendering (50% opacity, `statusMissing` magenta border, `✕` corner glyph at `spacing.lg`). New `@tauri-apps/plugin-dialog` wired in (Cargo.toml + capability + `tauri_plugin_dialog::init()`); HTML-overlay context-menu and toast patterns inlined per ADR-003 (no new ADR — component-internal until they proliferate); `project_removed` handler established as THE canonical listener for canvas-005b to reuse without duplication. `pnpm check` 0/0/0. Commit `8ff6a1e`. (2026-05-15)
- [canvas-006-live-add-race-on-concurrent-project-added](done/canvas-006-live-add-race-on-concurrent-project-added.md) — `bug` — Serialised the `project_added` → `addLiveProject` path through a single promise chain (`liveAddChain` + `enqueueLiveAdd`); N back-to-back arrivals now process strictly sequentially so each tile lands at a distinct spiral slot in memory and in `tile_positions`. Post-await `findProject` re-check kept as defence in depth. Mount path + `project_missing` branch untouched. `pnpm check` 0/0/0. Commit `f83222c`. (2026-05-15)
- [canvas-002-render-multiple-project-tiles](done/canvas-002-render-multiple-project-tiles.md) — `feature` — `Canvas.svelte` restructured from single-tile → keyed-by-`project_id` collection. New pure `tile-layout.ts` (square-spiral auto-placement, no Svelte/Pixi imports). Shared window-level drag controller replaces N per-tile listeners. Per-id event routing; `project_added` idempotent live-add; `sceneWorldBounds` unions every tile. `pnpm check` 0/0/0. Commit `e690fd3`. (2026-05-15)
- [canvas-001-targeted-canvas-updates](done/canvas-001-targeted-canvas-updates.md) — `type: feature`. Canvas patches its `ProjectSnapshot` in place from the fine-grained FS events (`task_moved`/`added`/`removed`, `bc_appeared`/`disappeared`) instead of re-fetching; coarse `AgentheimChanged` retired → renamed `ResyncRequired` (lag-only resync signal). ADR-009 amended in place. Commit `5fa7080`.
<!-- done-list:end -->

## Research

<!-- research-list:start -->
- [canvas-perf-2026-05-17](../../knowledge/research/canvas-perf-2026-05-17/README.md) — Canvas pan/zoom sluggishness investigation. Identifies the dominant per-frame cost (PixiJS ticker unconditionally rebuilding the scene graph ~60 Hz), ranks three remaining hotspots with proposed fixes + effort estimates, documents the Marco-runnable reproducer protocol (Chrome trace + JS console snapshot). Authored for canvas-014 (2026-05-18).
<!-- research-list:end -->
